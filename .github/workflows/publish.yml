on:
    workflow_dispatch:
      inputs:
        package:
          required: true
          type: string

permissions:
  contents: write
  id-token: write

name: Publish

jobs:
    release-core:
        uses: ./.github/workflows/release-package.yml
        with:
            package: ${{ inputs.package }}
